<div class="card m-3">
  <div class="card-header">
    <h2 class="card-title"><i class="ti ti-transfer me-2"></i>{{ __('Migration profiles', 'ticketmigration') }}</h2>
  </div>
  <div class="card-body">
    <div class="alert alert-info" role="status">
      {{ __('Profile creation and CSV preview are being enabled in this development milestone.', 'ticketmigration') }}
    </div>
    <p class="text-muted mb-0">
      {{ __('Uploaded sources are streamed from protected GLPI plugin storage and are never copied into the PHP session.', 'ticketmigration') }}
    </p>
  </div>
</div>
